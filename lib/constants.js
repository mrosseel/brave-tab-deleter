/**
 * Shared constants for the extension
 */

// Render debounce delay in milliseconds
export const RENDER_DEBOUNCE_MS = 300;

// Auto-ordering check interval in milliseconds
export const AUTO_ORDERING_CHECK_INTERVAL_MS = 1000;

// Ghost group countdown update interval in milliseconds
export const GHOST_COUNTDOWN_INTERVAL_MS = 1000;
